#include "image.h"
#include <iostream>
#include <fstream>
using namespace std;

int main(){
  cout << "lol\n";
  // Pixel a;
  //Image img(20,20);
  // ofstream fout;
  // fout.open("myimg.ppm");
  // if (!fout){ cerr << "No fout\n";
  //   return -1;}
  // img.writeppm(fout);
  // fout.close();
 return 0;
}
